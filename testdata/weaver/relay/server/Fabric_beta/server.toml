# Name of the relay
name = "Fabric_beta"
# Port number for relay grpc server. e.g. 9080
port="20042"
# Host address for grpc server. e.g. 0.0.0.0
host="0.0.0.0"

db_path="db/Fabric_beta/requests"
# This will be replaced by the task queue.
remote_db_path="db/Fabric_beta/remote_request"

# FOR TLS
cert_path="credentials/fabric_cert.pem"
key_path="credentials/fabric_key"
# tls=true

[networks]
[networks.Fabric_alpha]
network="Fabric"

[relays]
[relays.Fabric_alpha]
hostname="localhost"
port="20040"

[drivers]
[drivers.Fabric]
hostname="localhost"
port="20043"
