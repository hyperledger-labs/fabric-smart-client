name: Bug Report
description: Report a bug encountered while using the Fabric Smart Client
labels: ['status: awaiting triage']
projects: ["hyperledger-labs/8"]
type: Bug
body:
  - type: textarea
    id: description
    attributes:
      label: What happened?
      description: |
        Briefly describe what is wrong. What did you observe, and what did you expect instead?
        Include relevant file paths, class names, or method names if you know them.
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: How can we reproduce it?
      description: |
        Provide clear, numbered steps that consistently reproduce the issue.
        Include any relevant code, command-line invocations, or configuration.
    validations:
      required: true

  - type: textarea
    id: environment
    attributes:
      label: Environment
      description: Provide details about the environment where the bug occurred.
      value: |
        - FSC version (e.g., latest main, v0.9.0):
        - Go Version (e.g., go1.25.1 darwin/arm64):
        - Operating system (e.g., Linux, MacOS):
    validations:
      required: true

  - type: textarea
    id: additional
    attributes:
      label: Additional Information
      description: |
        Anything else that might help: logs, screenshots, related issues, bisected commits, etc.
    validations:
      required: false
